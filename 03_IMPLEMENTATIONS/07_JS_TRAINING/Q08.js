/**
 * Mastery Drill: No.08 (Comments & Documentation)
 * --------------------------------------------------
 * 【お題】1行コメントと複数行コメントを記述する
 * --------------------------------------------------
 */

// --- [1行コメント] ---
// JS/ES5/ES6/GO すべて共通
// var x = 10; // ← このように行の末尾にも書ける


// --- [複数行コメント] ---
/*
  これも JS/ES5/ES6/GO すべて共通。
  一時的なコードの無効化（コメントアウト）によく使う。
*/


// --- [JS特有: JSDoc (ドキュメント生成用)] ---
/**
 * 合計を計算する関数 (ES5スタイル)
 * @param {number} a - 第一引数
 * @param {number} b - 第二引数
 * @returns {number}
 * * GOなら関数の直上に // FunctionName ... と書くのが作法だが、
 * JSではこのように /** で始める「JSDoc」が標準。
 */
function add(a, b) {
    return a + b;
}

/* ▼ 言語別「コメント」比較表
  -----------------------------------------------------------------------
  | 種類         | JS (ES5/6)     | GO (Golang)      | 備考                |
  | :----------- | :------------- | :--------------- | :------------------ |
  | 1行          | //             | //               | 全く同じ            |
  | 複数行       | /* ... * /     | /* ... * /       | 全く同じ            |
  | ドキュメント | /** ... * /     | // (関数名の直上) | JSはアスタリスク2つ |
  -----------------------------------------------------------------------
*/

/**
 * 【解法ポイント】
 * 1. 基本的な記号はGOと同じなので、Macの `Cmd + /` や `Shift + Opt + A` がそのまま役立つ。
 * 2. JS実務では、単なるコメントより「JSDoc（/** ... */）」が重要。
 * 3. 理由：VS Codeなどのエディタで関数を呼び出す際、引数の型（number等）をツールチップで教えてくれるようになるから。
 * 1行ハック：「JSDocは単なるメモではなく『エディタの補完機能を覚醒させる設定ファイル』だと心得よ」【1行ハック】「実装の『どう（How）』ではなく、背景の『なぜ（Why）』をコメントに残し、JSDocで型の曖昧さを封じ込めよ」
 */