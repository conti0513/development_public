/**
 * Mastery Drill: No.18 (Modulo Operator)
 * --------------------------------------------------
 * 【シナリオ】
 * 1. 偶数か奇数かを判定せよ。
 * 2. 大量ループの中で、10件ごとに「進捗ログ」を出力せよ。
 * --------------------------------------------------
 */

// --- 1. 偶奇判定の基本 ---
var num = 7;
if (num % 2 === 0) {
    console.log(num + ' は偶数です');
} else {
    console.log(num + ' は奇数です');
}

// --- 2. 実務ハック：一定間隔での処理実行 ---
var totalRecords = 35;
console.log('--- バッチ処理開始 ---');

for (var i = 1; i <= totalRecords; i++) {
    // 実際の重い処理（DB更新など）のダミー
    // ... logic ...

    // 10件ごとに進捗を表示
    if (i % 10 === 0) {
        console.log('>> 進捗報告：' + i + ' 件の処理が完了しました。');
    }
}

console.log('全 ' + totalRecords + ' 件の処理が終了しました。');

/**
 * 【ハックのポイント】
 * 剰余算（%）は、ループの中で「特定の周期」を作り出す魔法です。
 * ServiceNowのバックグラウンドスクリプトで、数万件のレコードを回す際、
 * 全部のログを出すと重すぎるため `i % 100 === 0` で間引くのは「必須の作法」です。
 */