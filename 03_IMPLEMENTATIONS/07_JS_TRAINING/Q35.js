### 📂 Q35.js (while Loop / 条件継続ループ)

```javascript
/**
 * Mastery Drill: No.35 (while Loop / 条件による反復)
 * --------------------------------------------------
 * 【シナリオ】
 * 1. カウンタが上限に達するまで処理を繰り返せ。
 * 2. ServiceNowの GlideRecord 走査のような「次のデータがある限り続ける」
 * ロジックの構造をシミュレートせよ。
 * --------------------------------------------------
 */

// --- [1. 基本の形] ---
var count = 0;
while (count < 3) {
    console.log("Q35 Loop: " + count);
    count++; // 【超重要】これを忘れると無限ループでブラウザやサーバーが死ぬ
}


// --- [2. 実務のシミュレーション（データ走査）] ---
// 配列を「キュー（待ち行列）」に見立てて、中身がなくなるまで処理する
var queue = ["Task1", "Task2", "Task3"];

while (queue.length > 0) {
    var currentTask = queue.shift(); // 先頭を1つ取り出す（破壊的操作）
    console.log("Q35 Processing: " + currentTask + " (Remaining: " + queue.length + ")");
}

/**
 * 【ハックのポイント：精読時の眼力】
 * 1. 無限ループの恐怖:
 * while 文の中で「終了条件に向かう更新（count++ など）」が行われていない
 * コードを見つけたら、それはシステムをフリーズさせる重大バグです。
 * 2. ServiceNowでの定番パターン:
 * SNエンジニアが最も目にする `while(gr.next()) { ... }` は、まさにこれです。
 * 「次のレコードが存在する（trueを返す）間はループを回す」という定石です。
 */

```

---

### 💡 現場での「勝ち筋」

「気まずいスクリプト」の中で `while` が使われている場合、それは**「終わりのタイミングが予測できない不定形なデータ」**を扱っているサインです。

* **CSVの1行ずつ読み込み**
* **APIのページネーション（次のページがある限り叩く）**
* **DBの全件ヒット処理**

これらを精読する際は、「何をもってこのループは終わるのか？（終了条件）」を真っ先に特定するのが、解読のショートカットになります。

---

次は、同じく空ファイルになっている **Q36（数値妥当性：isNaN）** に進みますか？
（これで Q13, Q34, Q35 が埋まりました！）