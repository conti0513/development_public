### Go言語実戦トレーニング：全72問リスト

#### 【第1章：基本の制御と型】

1. **Q01：** 変数の宣言と表示（`var` vs `:=` と型推論の使い分け）
2. **Q02：** 四則演算（`int` と `float64` の混在計算と明示的キャスト）
3. **Q03：** 剰余計算（偶数・奇数判定と `if` での剰余活用）
4. **Q04：** 比較演算子（ブール値の扱いと厳格な型比較）
5. **Q05：** if文：基本（`if score := 80; score >= 80` の前置宣言スコープ）
6. **Q06：** if文：多分岐（`else if` の活用と可読性の限界）
7. **Q07：** switch文：基本（暗黙の `break` と、複数条件のカンマ区切り）
8. **Q08：** switch文：応用（値を持たない `switch` と `fallthrough` の使い所）
9. **Q09：** for文：基本（1〜10の表示、Go唯一のループ構文）
10. **Q10：** for文：合計値の算出（ループ外変数への蓄積と生存期間）
11. **Q11：** for文：制御（`continue` を使った偶数のみの抽出）
12. **Q12：** for文：脱出（`break` を使った閾値超えの終了）
13. **Q13：** while文の代用（条件式のみの `for` ループ）
14. **Q14：** FizzBuzz問題（Goらしい簡潔な条件分岐の記述）
15. **Q15：** 三項演算子の代用（存在しない構文を `if-else` で安全に書く）

#### 【第2章：配列・関数・構造】

16. **Q16：** 二重ループ：九九の表（入れ子ループと `fmt.Printf` での整形）
17. **Q18：** 二重ループ：ピラミッド（文字列の繰り返しとループ制御）
18. **Q19：** 関数の基本（引数と戻り値の型定義、`func` 構文）
19. **Q20：** 複数の戻り値（名前付き戻り値と、エラーを返す作法）
20. **Q21：** 可変長引数（`...` を使ったスライスの受け渡し）
21. **Q21b：** Generics入門（`any` や `comparable` を使った型パラメータの基礎）
22. **Q22：** スライスの基本（`make` による初期化と `append` の動作）
23. **Q23：** スライスの操作（切り出し `s[low:high]` による要素削除）
24. **Q24：** 線形探索（スライス内の要素検索とインデックス返却）
25. **Q25：** rangeの活用（インデックスと値の取得、ブランク識別子 `_`）
26. **Q26：** スライスのソート（`slices.Sort` パッケージの利用）
27. **Q27：** 最大値・最小値（スライス走査による統計計算）
28. **Q28：** 重複削除（Mapのキーを利用した高速な集合処理）
29. **Q29：** スライスのアンパック（`...` を使ったスライス同士の結合）
30. **Q30：** 多次元スライス（行列データの表現と走査）

#### 【第3章：データ構造とバリデーション】

31. **Q31：** Mapの基本（キー・値ペアの宣言と `make`）
32. **Q32：** Mapの操作（要素の存在確認 `v, ok := m[k]` と `delete`）
33. **Q33：** Mapの反復（順序がランダムになる仕様の確認と対策）
34. **Q34：** 構造体（Struct）の基本（独自型の定義と初期化）
35. **Q35：** 構造体のスライス（オブジェクトのリスト管理）
36. **Q36：** 数値変換バリデーション（`strconv.Atoi` とエラーハンドリング）
37. **Q37：** ゼロ値の判定（`string` や `int` の初期状態と必須チェック）
38. **Q38：** 正規表現バリデーション（`regexp` パッケージによる形式チェック）
39. **Q39：** 標準入力の取得（`fmt.Scan` と `bufio.NewScanner`）
40. **Q40：** 乱数生成（`math/rand/v2` を使った現代的な乱数生成）

#### 【第4章：実用ロジックと標準パッケージ】

41. **Q41：** 文字列とルーン（バイト数と文字数 `utf8.RuneCountInString` の違い）
42. **Q43：** 文字列の分割・結合（`strings.Split` と `strings.Join`）
43. **Q44：** 日付のフォーマット（Layout `2006-01-02` の指定方法）
44. **Q45：** 日付の計算（`Add`, `Sub`, `Before/After` による期間判定）
45. **Q46：** Unixタイム（Unix時間からの変換と生成）
46. **Q47：** 数値整形（カンマ区切りやパディング出力）
47. **Q48：** 丸め処理（`math` パッケージによる四捨五入・切り上げ・下げ）
48. **Q49：** スライスのシャッフル（`slices.SortFunc` 以外のランダム操作）
49. **Q50：** ファイル読み込み（`os.ReadFile` とエラー処理）
50. **Q51：** ファイル書き込み（`os.WriteFile` とパーミッション）
51. **Q52：** CSV操作（`encoding/csv` による読み書き）
52. **Q53：** JSONのパース（構造体タグと `json.Unmarshal/Marshal`）
53. **Q54：** エラーハンドリング（`errors.New` と `fmt.Errorf`）
54. **Q55：** エラーのラップ（`%w` を使ったエラーチェーンと `errors.Is/As`）
55. **Q56：** HTTPリクエスト（`net/http` クライアントとレスポンス処理）
56. **Q57：** 環境変数（`os.Getenv` とデフォルト値の設定）

#### 【第5章：Goの真髄：インターフェースと設計】

58. **Q58：** メソッド：基本（構造体に紐付く関数の定義）
59. **Q59：** ポインタレシーバ（内部状態を書き換えるメソッド）
60. **Q60：** 値レシーバ（値を書き換えない安全なメソッド）
61. **Q61：** コンストラクタ関数（`NewStruct` パターンの慣習）
62. **Q62：** インターフェースの基本（振る舞いの抽象化 `interface`）
63. **Q63：** ダックタイピング（明示的な実装宣言なしに満たされる要件）
64. **Q64：** 構造体の埋め込み（Embeddingによるコード再利用）
65. **Q65：** カプセル化（大文字・小文字による公開範囲制御）
66. **Q66：** 現代的なパッケージ管理（`go mod` と `internal` ディレクトリ）
67. **Q67：** 外部ライブラリ導入（`go get` と `go.sum` の管理）
68. **Q68：** ユニットテスト（`testing` パッケージと `Table-driven tests`）
69. **Q69：** 総合演習：ATMシステム（ポインタとエラー処理の集大成）
70. **Q70：** 総合演習：在庫管理（JSON永続化とスライス操作）
71. **Q71：** 総合演習：認証処理（Mapによる会員管理とセキュリティ）
72. **Q72：** 総合演習：構造化ログ（`log/slog` と `defer` によるクリーンアップ）

---
