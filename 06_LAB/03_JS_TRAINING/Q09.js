/**
 * Mastery Drill: No.09 (Logical Operators)
 * --------------------------------------------------
 * 【お題】status が 'active' かつ age が 20 より大きい
 * --------------------------------------------------
 */

var status = 'active';
var age = 25;

// --- [1. 基本の条件分岐（GO/PHPと同じ）] ---
if (status === 'active' && age > 20) {
    console.log('条件クリア！');
}

// --- [2. 実務で超使う「デフォルト値」代入] ---
// JSの || は「左がダメ（空とかnull）なら、右を使う」という機能があります。
var input = ""; 
var name = input || "名無しさん"; // inputが空文字なので "名無しさん" が入る

console.log('名前: ' + name);

/* ▼ 3言語の「デフォルト値」の書き方比較
  -----------------------------------------------------------------------
  | 言語  | 書き方                                  | 特徴              |
  | :--- | :------------------------------------- | :---------------- |
  | **JS** | `var x = input || "default";`          | 最も簡潔だが、0も消える |
  | **PHP**| `$x = $input ?: "default";`            | エルビス演算子と同じ |
  | **GO** | `if input == "" { x = "default" }`     | 厳格。1行では書けない |
  -----------------------------------------------------------------------
*/

/**
 * 【解法ポイント】
 * 1. 記号（&&, ||, !）は全言語共通なので安心。
 * 2. JSでは || を使って「もし値が空ならこれを入れといて」というガードを1行で書く。
 * 3. ただし、数値の 0 も「ダメな値（false）」と判定されてしまう。
 * 「数量 0個」を許容したい時に || を使うと、デフォルト値に上書きされるバグが起きる。
 * 1行ハック：「デフォルト値代入の || は便利だが、数値 0 や偽値 false を『有効なデータ』として扱いたい場面では、バグの温床となる」
 */